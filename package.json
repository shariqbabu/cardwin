{
  "name": "cardwin-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cardwin serverless API — Vercel functions only",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "20.x"
  }
}
